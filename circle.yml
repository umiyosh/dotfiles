machine:
  timezone:
    Asia/Tokyo
dependencies:
  pre:
    - sudo apt-get install zsh
    - sudo apt-get install vim
  post:
    - ./setup.sh
    - zsh -c 'exit'
    - vim +qall
test:
  pre:
    - zsh -c 'exit'
    - vim +qall
